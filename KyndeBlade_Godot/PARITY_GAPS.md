# Unity ↔ Godot parity gaps

Living document for **intentional** or **known** differences between the **Unity oracle** ([`ProjectArchive/UnityKyndeBlade/KyndeBlade_Unity`](../ProjectArchive/UnityKyndeBlade/KyndeBlade_Unity)) and this **Godot Steam / retail** build. If behavior disagrees and the row is **not** listed here, treat Unity + [`PLAYABLE_SLICE.md`](../ProjectArchive/UnityKyndeBlade/KyndeBlade_Unity/Assets/KyndeBlade/Docs/PLAYABLE_SLICE.md) / [`DEMO_RUN.md`](../ProjectArchive/UnityKyndeBlade/KyndeBlade_Unity/Assets/KyndeBlade/Docs/DEMO_RUN.md) as oracle until updated.

| Area | Unity | Godot (this project) | Notes |
|------|--------|----------------------|-------|
| Save format | `PlayerPrefs` JSON (`KyndeBlade_Save`) | `user://kyndeblade_save.cfg` + **autosave** `user://kyndeblade_autosave.cfg` (`ConfigFile`); legacy **`kyndeblade_demo_save.cfg`** auto-migrates once | Core slice fields match behaviour; Godot adds **Piers metadata** (`piers_text_edition`, `narrative_phase_id`, `location_visit_counts`, `fair_field_return_count`, `dream_iteration`) from `SaveService.SAVE_VERSION` **2** — see [`STEAM_BUILD.md`](STEAM_BUILD.md). Not byte-compatible with Unity. |
| Settings | `KyndeBlade_Settings_*` keys | `user://kyndeblade_settings.cfg` (legacy `kyndeblade_demo_settings.cfg` migrates on first read) | Volume + fullscreen. |
| Combat RNG / defense | Unity implementation | Deterministic swing/feint by window index + **`feint_pattern_offset`** = `posmod(ethical_misstep_count + sum(read-text feint deltas), 2)` (odd misstep or text skew → first window can be **feint**) | **Godot-first consequence** until Unity matches. Documented in [`STEAM_BUILD.md`](STEAM_BUILD.md). Headless: [`tests/combat_scenarios.gd`](tests/combat_scenarios.gd) incl. `misstep_inverts_first_defensive_window`. |
| Combat **presentation** | `CombatUI`, `ParryDodgeZoneIndicator`, orthographic stage + `EnsureCharacterVisual` quads | **Staged parity:** `CombatStage` + polygon placeholders, `ParryDodgeEye` + `React!` label, `window_duration` / `window_phase_t()` on `CombatManager`; optional `show_foreground_hazard` on [`combat_presentation.gd`](scripts/combat_presentation.gd); **Godot-only** dreamer **ink physics** blobs on strike/dodge/parry ([`dreamer_ink_physics.gd`](scripts/dreamer_ink_physics.gd)) | Not sprite/asset parity; matches Unity **read** (Lane B void, timed window phases). Ink is authorial Godot flavor. See [`docs/TDAD_COMBAT_PRESENTATION_PLAN.md`](docs/TDAD_COMBAT_PRESENTATION_PLAN.md). |
| Rendering | Built-in RP, manuscript shaders | Godot 4 default 2D | Shader parity not targeted in this slice. |
| Piers combat hazards (W6) | `PiersHazardConfig` per turn / location | `CombatManager.apply_piers_hazard_damage()` returns **0** | Hook exists on enemy turn; no hazard data loaded yet. |
| Kynde / break / status stack (W6) | Full Unity resource systems | Not in current slice | Track subsystems as you port M5 TDAD folders. |
| Meta flags (W7) | `GameStateManager` / poverty / aging | `ethical_misstep_count` **increments** on hub **Mede** counsel before Fair Field combat; **feint pattern** reads it. `has_ever_had_hunger` set by **Name hunger** counsel and on **combat defeat**; **parry window ms** and **parry stamina cost** then gain a **deterministic but varying** tax (`PlayerMovesetModifiers` whimsy seed from location + dream/fair-field counts + misstep). Hub hint + flavor reflect the odd, shifting bite. | **Godot leads** Unity on this slice until equivalent branches ship. |
| Piers versioning / symbols / repetition | Unity `GameStateManager` (no symbol registry in slice) | `text_version_manifest.json`, `piers_symbols.json`, `locations_registry` optional `piers_symbols`; `GameState` + save: edition, phase, visit counts, `fair_field_return_count`, `dream_iteration`; `NarrativeContext` stub for arrival variant keys. | **Godot leads** on symbol/version metadata until Unity mirrors fields. |
| Audio (W7) | Unity mixers / themes | `Music` + `SFX` buses via `AudioBusSetup` autoload; **procedural** defensive-window beeps on `defensive_window_started` (real vs feint pitch); **kill** thump + slow-mo desaturate on victory | No streamed music yet; Master volume matches settings. Policy: [`docs/GODOT_AUDIO_DESIGN.md`](docs/GODOT_AUDIO_DESIGN.md); inventory: [`docs/ASSET_LICENSES.md`](../docs/ASSET_LICENSES.md). |
| Map graph (W2) | Full `WorldMapManager` + all `LocationNode` assets | Hub stub + `slice_locations.json` + `exported_from_unity.json` | Export pipeline proves data path; expand loaders per wave. |
| Narrative beats (W3) | Full `NarrativeManager` / branching | `tower_intro.tscn` + hub flavor from export `arrival_beat_*` + **pre-combat counsel** (Trewthe / Mede / Name hunger) on Fair Field | Minimal **branch** (stats + combat read); not full Unity narrative graph. |
| Medieval text → moveset | Unity hooks TBD | [`data/medieval_text_unlocks.json`](data/medieval_text_unlocks.json) + `GameState.read_medieval_text_ids` (save); [`MedievalTextCatalog`](scripts/medieval_text_catalog.gd) / [`PlayerMovesetModifiers`](scripts/player_moveset_modifiers.gd) feed `CombatManager` strike/dodge/feint; **fae / GK** weights are data hooks for future field spawns (see [`docs/MEDIEVAL_TEXT_MOVESET_DESIGN.md`](docs/MEDIEVAL_TEXT_MOVESET_DESIGN.md), repo [`docs/UNITY_STORY_AND_SPAWN_DIGEST.md`](../docs/UNITY_STORY_AND_SPAWN_DIGEST.md)). | **Godot-first** progression layer; some grants are **detrimental** by design (`hidden_drawback` in JSON). |
| Hub / map UI copy | Unity `WorldMapManager` chrome | Archaic-modern hub **title**, **hint**, **travel** strings in Godot | **Intentional** (cult-ship voice). Not matched line-for-line to Unity. See [`docs/CULT_SHIP_BAR.md`](docs/CULT_SHIP_BAR.md). |
| Authorial “scar” (optional) | Unity equivalent for chosen screen | **TBD** — track in [`docs/AUTHORIAL_OVERRIDE.md`](docs/AUTHORIAL_OVERRIDE.md) | When filled, one Godot-first aesthetic override is explicitly **not** parity until promoted. |

| Crawl overworld + 3D combat stage (future) | `WorldMapManager` + current Unity combat presentation | **Planned Godot:** fake-voxel crawl → pop-out [`combat.tscn`](../KyndeBlade_Godot/scenes/combat.tscn); migrate combat to **full-screen `Node3D` + `Camera3D`** + `CanvasLayer` HUD — see [`docs/CRAWL_PROTOTYPE_FUTURE.md`](docs/CRAWL_PROTOTYPE_FUTURE.md), [`docs/COMBAT_VOXEL_STAGE_FUTURE.md`](docs/COMBAT_VOXEL_STAGE_FUTURE.md). Headless: scene mount smoke only until crawl scene exists. | **Not started** — hub + 2D combat slice stand in. Optional **GPU** visual CI when 3D ships — document in [`docs/CI_GODOT_TESTS.md`](../docs/CI_GODOT_TESTS.md). |

Add a row for each new subsystem you port; use **Intentional** or **TODO: align** in Notes.
